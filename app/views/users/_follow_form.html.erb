<% unless current_user?(@user) %>
    <div id="follow_form" style="margin-bottom: 10px;">
      <% if current_user.following?(@user) %>
          <%= render 'unfollow' %>
      <% else %>
          <%= render 'follow' %>
      <% end %>
    </div>
<% end %>